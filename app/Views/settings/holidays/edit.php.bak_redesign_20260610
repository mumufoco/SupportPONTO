<?= $this->extend('layouts/main') ?>

<?= $this->section('title') ?>Editar Feriado<?= $this->endSection() ?>

<?= $this->section('content') ?>
<div class="container-fluid py-4">

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-4">
        <div>
            <h1 class="h4 mb-1"><i class="bi bi-pencil me-2"></i>Editar Feriado</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item"><a href="<?= sp_route_url('settings') ?>">Configurações</a></li>
                    <li class="breadcrumb-item"><a href="<?= sp_route_url('settings.holidays') ?>">Feriados</a></li>
                    <li class="breadcrumb-item active"><?= esc($holiday->name) ?></li>
                </ol>
            </nav>
        </div>
        <a href="<?= sp_route_url('settings.holidays') ?>" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-2"></i>Voltar
        </a>
    </div>

    <div class="card" style="max-width: 640px;">
        <div class="card-body">
            <form method="POST" action="<?= sp_route_url('settings.holidays.update', (int) $holiday->id) ?>">
                <?= csrf_field() ?>

                <div class="mb-3">
                    <label for="name" class="form-label">Nome <span class="text-danger">*</span></label>
                    <input type="text" name="name" id="name" class="form-control"
                           value="<?= esc(old('name', $holiday->name)) ?>" required maxlength="255">
                </div>

                <div class="row g-3 mb-3">
                    <div class="col-md-6">
                        <label for="date" class="form-label">Data <span class="text-danger">*</span></label>
                        <input type="date" name="date" id="date" class="form-control"
                               value="<?= esc(old('date', $holiday->date)) ?>" required>
                    </div>
                    <div class="col-md-6">
                        <label for="type" class="form-label">Tipo <span class="text-danger">*</span></label>
                        <select name="type" id="type" class="form-select" required>
                            <?php
                            $types = ['national' => 'Nacional', 'state' => 'Estadual', 'municipal' => 'Municipal', 'company' => 'Empresa', 'non_working' => 'Dia Não Trabalhado'];
                            $currentType = old('type', $holiday->type);
                            foreach ($types as $val => $label):
                            ?>
                                <option value="<?= esc($val) ?>" <?= $currentType === $val ? 'selected' : '' ?>><?= esc($label) ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="description" class="form-label">Observação</label>
                    <input type="text" name="description" id="description" class="form-control" maxlength="500"
                           value="<?= esc(old('description', $holiday->description ?? '')) ?>" placeholder="Opcional...">
                </div>

                <div class="row g-3 mb-4">
                    <div class="col-md-6">
                        <div class="form-check form-switch mt-2">
                            <input class="form-check-input" type="checkbox" name="recurring" id="recurring"
                                   value="1" <?= old('recurring', $holiday->recurring ?? 0) ? 'checked' : '' ?>>
                            <label class="form-check-label" for="recurring">
                                <strong>Recorrente</strong><br>
                                <small class="text-muted">Repetir todo ano nesta data</small>
                            </label>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-check form-switch mt-2">
                            <input class="form-check-input" type="checkbox" name="blocks_punch" id="blocks_punch"
                                   value="1" <?= old('blocks_punch', $holiday->blocks_punch ?? 1) ? 'checked' : '' ?>>
                            <label class="form-check-label" for="blocks_punch">
                                <strong>Bloquear ponto</strong><br>
                                <small class="text-muted">Impede registro sem autorização do admin</small>
                            </label>
                        </div>
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-lg me-1"></i>Salvar Alterações
                    </button>
                    <a href="<?= sp_route_url('settings.holidays') ?>" class="btn btn-outline-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
<?= $this->endSection() ?>
