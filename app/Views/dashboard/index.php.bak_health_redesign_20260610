<?= $this->extend('layouts/main') ?>

<?= $this->section('title') ?>Dashboard<?= $this->endSection() ?>

<?= $this->section('content') ?>
<?php
$role = $role ?? 'funcionario';
$profileCards = $profileCards ?? [];
?>
<div class="container-fluid sp-module-stack">
    <?= view('components/page_header', [
        'title' => 'Dashboard',
        'subtitle' => 'Painel inteligente com foco em ação, acompanhamento e resolução rápida de pendências.',
        'icon' => 'bi bi-grid-fill',
        'actions' => [
            ['label' => 'Pendências', 'icon' => 'bi bi-clipboard-check-fill', 'url' => site_url('operations/pending-center')],
            ['label' => 'Ações rápidas', 'icon' => 'bi bi-lightning-charge-fill', 'url' => site_url('operations/action-center')],
            ['label' => 'Notificações', 'icon' => 'bi bi-bell-fill', 'url' => site_url('operations/notifications-center')],
        ],
    ]) ?>

    <div class="sp-pending-grid">
        <?php foreach ($profileCards as $card): ?>
            <a class="sp-pending-card" href="<?= sp_safe_url($card['url']) ?>">
                <div class="sp-pending-card__icon"><i class="<?= esc($card['icon']) ?>"></i></div>
                <div class="sp-pending-card__count"><?= esc($card['value']) ?></div>
                <div class="sp-pending-card__label"><?= esc($card['label']) ?></div>
                <div class="sp-pending-card__desc"><?= esc($card['desc']) ?></div>
            </a>
        <?php endforeach; ?>
    </div>

    <?= view('partials/dashboard_quick_actions') ?>
</div>
<?= $this->endSection() ?>
