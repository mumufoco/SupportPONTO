# DeepFace API Requirements
# Python 3.10–3.11 recomendado para TensorFlow 2.15/DeepFace fixados

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0
Werkzeug==3.0.1

# DeepFace and dependencies
deepface==0.0.89
tensorflow==2.15.0
# tf-keras não é pinado por padrão pelo tensorflow==2.15.0 (fica como dependência
# opcional/transitiva) e pode ser puxado independentemente para uma versão mais
# recente por outro pacote — encontrado em produção com tf_keras 2.21.0 instalado
# junto de tensorflow 2.15.0, causando ImportError (tf_keras exigia
# google.protobuf.runtime_version, ausente na faixa de protobuf que o TF 2.15
# exige). Fixado na mesma série 2.15.x do tensorflow para não repetir o crash-loop.
tf-keras==2.15.1
opencv-python-headless==4.8.1.78
Pillow==10.1.0

# Additional ML libraries
numpy==1.24.3
pandas==2.1.3

# Utilities
python-dotenv==1.0.0
gunicorn==21.2.0

# Criptografia em repouso das fotos faciais cadastradas (CRIT-07 na auditoria)
cryptography==42.0.5

# Image processing
mtcnn==0.1.1
retina-face==0.0.13

# Validation
jsonschema==4.20.0

# Logging
colorlog==6.8.0
